// EdgeOne Pages 部署配置
// 主要配置在 edgeone.json 中

export default {
  // 全局配置
  global: {
    // 默认响应头
    defaultHeaders: {
      'Access-Control-Allow-Origin': '*',
      'X-Powered-By': 'EdgeOne Pages'
    }
  }
}; 